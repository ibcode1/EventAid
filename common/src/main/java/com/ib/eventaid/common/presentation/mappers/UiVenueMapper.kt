package com.ib.eventaid.common.presentation.mappers

/*
import com.ib.eventaid.common.domain.model.venue.Venue
import com.ib.eventaid.common.presentation.UIVenue
import javax.inject.Inject

class UiVenueMapper @Inject constructor():UiMapper<Venue, UIVenue> {
    override fun mapToView(input: Venue): UIVenue {
        return UIVenue(
            id = input.id,
            city = input.address.city,
            name = input.name,
            numUpcomingEvents = input.info.numUpcomingEvents
        )
    }
}*/
