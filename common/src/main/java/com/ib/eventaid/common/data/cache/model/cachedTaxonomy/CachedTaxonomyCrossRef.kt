package com.ib.eventaid.common.data.cache.model.cachedTaxonomy

import androidx.room.Entity
import androidx.room.Index

/*@Entity(primaryKeys = ["eventId","taxonomyId"],
indices = [Index("taxonomyId")])
data class CachedTaxonomyCrossRef(
    val taxonomyId:Int,
    val name:String,
    val rank:Int
)*/
